import React from 'react'

const Button = ({text}) => {
  return (
    <button className='px-7 py-3 font-inter font-medium text-sm rounded-3xl bg-[#FB432C] text-white'>{text}</button>
  )
}

export default Button